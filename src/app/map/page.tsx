import MapClientComponent from '@/components/MapClientComponent';

// Default export MUST be a function component
export default function MapPage() {
  return (
    <main className="map-container">
      <MapClientComponent />
    </main>
  );
}
